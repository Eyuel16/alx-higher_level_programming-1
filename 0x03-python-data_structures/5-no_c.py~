#!/usr/bin/python3
def no_c(my_string):
    final_str = ""
    for i in range (len(my_string)):
        if my_string[i] != 'c' or my_string[i] != 'C':
            final_str = final_str + my_string[i]
    return final_str
